AGENTS.md

このプロジェクト"qemu-brain"はsharp brain imx28系 ram128mb pw-aj2(pw-sh6系列)のemmcイメージ使いqemu上でbrainマシンとして実機デバイスをフル仮想化して起動させるためのプロジェクトである。

完成されたqemu-brainとしての理想とする姿、qemu-brainに求める必須項目は
1,brain実機マシンのハードウェアすべての1対1の完全仮想化。
2,emmcを起動した際、エラーや失敗が起きずに起動でき、中のアプリもすべてエラーやクラッシュ、フリーズなく起動でき、画面もきれいに表示され画面の内容を視認できること。

3,すべてのログを1つづつ様々なケース(アプリ起動、タッチ、入力、起動時、など)で取り、そのログの内容にqemu-brainの実装不足に起因するエラーや不具合、などが一切なくすべてのログで正常な状態が確認できること。

4,画面タッチにおいてタッチの握り潰しやタッチの取りこぼし、タッチのズレ、モデルの仕様とのズレ、想定される動作、タッチが反応するはずの場所でちゃんと反応が確認できるか、をテストした際にそれらの動作に一切問題が見られないこと。

5,すべてのbrainマシン実装ファイルにおいてモデル仕様とのズレがなく1対1での実装が確認できること

6,すべてのbrainマシンのモデルの実装においてスタブやnoop、応急的な実装、張りぼてや余計な補助などがすべて取り外されすべて実機仕様と1対1の実装であることが確認できること。

7,emmc内部に存在するハードウェアドライバに対応するモデルがすべてqemuに実装されていることが確認でき、駆動時にずれやバグがないということが確認できること。

これらの項目が同時にすべて達成できることを必須とする。

作業のサイクルは実装調査、テスト起動、集計、解析、調査、修正・実装、テスト、集計、調査、のサイクルを上記の7項目が達成されるまで繰り返すこと。

今の状態を保守するのではなくて上記の7項目の達成を最優先すること。

~/emmc配下に現状態のqemuと使用するemmcが配置されていることを確認すること。

初回はこの~/emmc配下のqemuを使いテストすること。
2回目からは修正を適用しビルドしてテスト&修正・実装を実施すること。

